<!--
Thanks for the pull request! A short description beats a perfect checklist —
if something below does not apply, just delete it.
-->

## What does this change?

<!-- What changes for users, and why. -->

Fixes #

## How did you verify it?

<!-- Commands you ran, or what you clicked through in the UI. -->

## Checklist

- [ ] `make test` and `make vet` pass
- [ ] Templates or styles touched? `make generate` and `make css` were run and
      the regenerated files are committed
- [ ] User-facing strings added to **both** `en.json` and `de.json`
- [ ] Documentation in `docs/` updated if behaviour or configuration changed
- [ ] Commit subject follows [Conventional Commits](https://www.conventionalcommits.org/)
      (`feat:`, `fix:`, `docs:`, … — with `!` for breaking changes)
